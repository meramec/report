.app-title.header-banner
  .title
    {{ report.title }}
  .subtitle
    %ul
      %li
        {{ report.subtitle }}
      %li{ 'ng-repeat' => 'nested in report.nestedSubtitle' }
        {{ nested }}
  .navigation

